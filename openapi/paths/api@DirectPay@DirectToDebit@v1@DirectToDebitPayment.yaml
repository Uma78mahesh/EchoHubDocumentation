post:
  tags:
    - DirectToDebit
  summary: Direct To Debit Payment Create
  description: | 
   This endpoint allows the API user to create a payment to the Provider.  
   The Payer is identified with the unique Payor reference ID.
  operationId: DirectToDebitPayment
  requestBody:
    description: This endpoint is used to create a Payment to the Provider.
    required: true
    content:
      application/json:
        schema:
          $ref: ../components/schemas/DirectToDebitPaymentCreate.yaml
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            $ref: ../components/schemas/DirectToDebitPaymentResponse.yaml
